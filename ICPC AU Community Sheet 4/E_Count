#include <iostream>
#include <string>
using namespace std;

int main()
{
    string s;
    cin >> s;

    long long int sum = 0;
    for (char c : s)
    {
        if (isdigit(c))
        {
            sum += (c - '0');
        }
    }

    cout << sum << endl;
    return 0;
}
