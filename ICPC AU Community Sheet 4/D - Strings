#include <iostream>
using namespace std;

int main()
{
    string a, b;
    cin >> a >> b;

    int len1 = a.length();
    int len2 = b.length();

    string newstring = a + b;

    swap(a[0], b[0]);

    cout << len1 << " " << len2 << endl;
    cout << newstring << endl;
    cout << a << " " << b << endl;

    return 0;
}
