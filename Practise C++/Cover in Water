#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {

        int n;
        cin >> n;

        string s;
        cin >> s;

        if (s.find("...") != string::npos)
        {
            cout << 2 << endl;
        }
        else
        {
            int empty = count(s.begin(), s.end(), '.');
            cout << empty << endl;
        }
    }

    return 0;
}
