#include <iostream>
using namespace std;
 
void sum(long long int x, long long int y)
{
    cout << x + y << endl;
}
 
int main()
{
    long long int x, y;
    cin >> x >> y;
    sum(x, y);
    return 0;
}
