#include<stdio.h>
int main() {
    int n;
    scanf("%d", &n);
 
    char a[n];
    scanf("%s", a);
 
    long long int sum=0;
    for(int i=0; i<n; i++) {
        char zero = '0';
        int digit = (int) a[i]- (int) zero;
        sum+=digit;
    }
    printf("%lld", sum);
 
 
return 0;
}
